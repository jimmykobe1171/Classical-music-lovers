const Playlist = require('../models/playlist');
const index = (req, res)=>{
console.log('this is from playlist index controller')
}
 module.exports = {
   index,
//     show,
//     new: newPlaylist,
//     create
  };
  